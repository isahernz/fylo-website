---
import { Image } from "astro:assets";
import HeroImage from "@/assets/illustration-intro.png";
---

<section>
  <div class="hero-background"></div>
  <div class="container flex max-w-[600px] flex-col gap-10 pt-10">
    <Image src={HeroImage} alt="Hero Ilustration" loading="eager" />
    <div class="flex flex-col gap-6 text-center">
      <h1 class="font-display text-3xl font-bold md:text-4xl">
        All your files in one secure location, accessible anywhere
      </h1>
      <p class="text-sm">
        Fylo stores all your most important files in one secure location. Access
        them wherever you need, share and collaborate with friends family, and
        co-workers.
      </p>
      <button
        class="relative rounded-full bg-gradient-to-tr from-accent-100 to-accent-200 px-12 py-3 font-bold text-white after:absolute after:left-0 after:top-0 after:h-full after:w-full after:rounded-full after:bg-white after:bg-opacity-0 after:transition-all after:duration-300 after:hover:bg-opacity-40"
        >Get Started</button
      >
    </div>
  </div>
</section>

<style>
  .hero-background {
    background: hsl(217, 28%, 15%) url(../assets/bg-curvy-mobile.svg);
    background-repeat: no-repeat;
    background-position: bottom center;
    background-size: contain;
    width: 100%;
    height: 100%;
    position: absolute;
    bottom: 11rem;
    z-index: -1;
  }

  @media (min-width: 768px) {
    .hero-background {
      background: hsl(217, 28%, 15%) url(../assets/bg-curvy-desktop.svg);
      background-repeat: no-repeat;
      background-position: bottom center;
      background-size: 100% 279px;
      width: 100%;
      height: 100%;
      position: absolute;
      bottom: 0;
    }
  }
</style>
